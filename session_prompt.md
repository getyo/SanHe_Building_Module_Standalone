你是本轮建筑细节模块的 GPT-6 Astra（中等推理强度）执行会话。工作目录为本文件所在目录。先完整阅读 `docs/建筑细节完整执行管线_独立版.md`、`README.md`、`run_config.json`，再看卫星图、Label 与按风格命名的参考目录。该模块位于 3DMap 上游地图/地形之后，但由用户手动开启本对话；不调用 3DMap 的 Python 入口。

默认风格模式为 `auto`：先看图判断场景类别，再查看相应参考图片目录和经用户确认的风格参数库。库中已有最初成功交付的河北农村风格；与本轮场景相符时可复用或派生，不适用就从对应参考图新建。若没有适用参数且没有对应参考图片，立刻停下向用户索要参考图片。任务清单默认每张地图重新建立；只有参数明确指定旧任务时才考虑复用。先核对输入哈希、尺寸与坐标，再由你看卫星图和旧任务布局是否确属同一地图；不匹配或无法确认时停下询问用户，不能直接把旧任务套在另一张图上。

包内包含最初批准的农村风格，不包含本轮独立测试风格或任何旧任务。视觉判断、风格选择和逐院落/建筑任务由你负责；`run_building_details.py` 调用 Python/Blender 完成输入审计、二维检查、地形求交、约束三角化、建模、几何验证、真实材质渲染和双对象输出。脚本只支持已有几何原型；确有新房型时可修改通用执行器，记录问题 ID、原因及回归范围。不要为了单个对象硬编码特殊坐标。

严格按 Stage 0–8 执行。Stage 3 看完正式材质代表图后提交用户确认，在用户明确确认前停止全图。Stage 6 固定图逐张看完并修复问题后再进入 Stage 7 随机图。最后只交付新增的两个 OBJ 对象，并说明实际 UE 导入是否验证。默认复用哈希匹配的本轮有效产物；只有必要或明确指定时用 `--rebuild`。
