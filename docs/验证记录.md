# 独立入口验证记录

隔离 QA 副本使用此前 Stage 3 的输入与任务验证通用执行链，随后清理 QA 副本；这些风格参数和任务没有进入独立版正式目录。

- 风格关口：入库前的隔离验证中，`rural_hebei` 有 21 张参考图且参数库为空时返回 `CREATE_FROM_REFERENCES`；不存在对应参数与参考目录时返回 `NEEDS_REFERENCE_IMAGES` 并停止。最初批准的农村风格入库后，默认 `auto` 模式会返回 `REVIEW_PRESETS`，由 Astra 看图决定是否适用。
- Blender：独立入口通过本机 `blender-launcher.exe` 启动 5.2.2 LTS，取得实际脚本回执。
- Stage 0–2：输入坐标与哈希审计通过；124 个院落、206 栋建筑、98 个院门的高层清单通过二维硬约束检查。
- Stage 3：代表编译、Blender 建模、实际导出 OBJ 逐三角面 Label/索引/法线检查通过；模型为 4,208 三角面，原 Building 基础没有进入细节 OBJ。四张真实材质代表图成功渲染，斜视和 5.5 米立面图已人工查看。

此记录只证明独立入口和代表样本路径可运行；不代替新会话对新地图的视觉规划和用户确认。Stage 4–8 的新独立版全图路径尚未在用户确认的风格上实跑，最终 UE 导入也仍需现场核对。

农村风格入库核对：`rural_sanhe_approved_v1.json` 除新增七张贴图的相对路径映射外，与最初批准的 `styles/rural_sanhe.json` 字段完全一致；七张贴图与原工作区逐字节一致。本轮独立 Stage 3 新生成的风格未入库。
